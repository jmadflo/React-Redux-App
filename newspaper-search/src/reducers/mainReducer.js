export const initialState = {
    data: [],
    searchInput: '',
    searchType: ''
}

export const mainReducer = (state = initialState, actions) => {
    switch(actions.type) {
        default:
            return state
    }
}